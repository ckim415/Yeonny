트리
1. 구조
 - 트리: 노드와 branch를 이용해서, 사이클을 이루지 않도록 구성한 데이터 구조
 - 어디에 사용?: 이진트리형태의 구조로, 탐색 알고리즘 구현을 위해 많이 사용됨
 
2. 알아둘 용어
 - 노드: 트리에서 데이터를 저장하는 기본 요소(데이터와 다른 연결된 노드에 대한 branch 정보 포함)
 - root node: 트리 맨 위에 있는 노드
 - branch: 가지 / 항상 위에서 아래로 연결
 - level: 최상위 노드를 level 0 으로 하였을 때, 하위 branch로 연결된 노드의 길이
 - parent node: 어떤 노드의 다음 레벨에 연결된 노드
 - child ndoe: 어떤 노드의 상위 레벨에 연결된 노드
 - leaf node(terminal node): child node가 하나도 없는 노드
 - sibling: 동일한 parent노드를 가진 노드
 - depth: 트리에서 node가 가질 수 있는 최대 level
 
3. 이진 트리와 이진 탐색 트리 (binary search tree)
 - 이진 트리: 노드의 최대 branch가 2인 트리
 - 이진 탐색 트리: 이진트리에 왼쪽 노드는 parent 노드 보다 작은값, 오른쪽 노드는 parent 노드보다 큰 값을 가지고 있음

4. 자료 구조 이진 탐색 트리의 장점과 주요 용도
 - 주요 용도: 데이터 검색
 - 장점: 탐색 속도를 개선할 수 있음
 - 단점: 이진 탐색 트리
